<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 <mapper namespace="ungmee.web.dao.UserDao">
 	
 	<select id="insert" resultType="ungmee.web.entity.User">
 		INSERT INTO MEMBER (EAMIL,PW,GENDER,ECHECK) VALUE(#{email},#{pw},#{gender},#{echeck})
 	</select>
 
 </mapper>